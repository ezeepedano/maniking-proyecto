<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('partials/head-base.ejs', {title: 'Listado de visitaos'})
        %>
    </head>

    <body>
        <header><%- include('partials/header.ejs') %></header>

        <main>
            <h2 class="title">Todos nuestros visitaos</h2>
            <section class="all-items">
            <% for (let i = 0; i < visitas.length; i++) { %> 
                <%- include("partials/visita", { visita: visitas[i] }) %> 
                <% } %>
            </section>
        </main>

    </body>
</html>
