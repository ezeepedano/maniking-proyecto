<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('partials/head-base.ejs', {title: 'Listado de visitaos'})
        %>
    </head>

    <body>
        <header><%- include('partials/header.ejs') %></header>

        <main>
            <h2 class="title">Resultados para <%= keyword%></h2>

            <% for (let i = 0; i < results.length; i++) { %> <%-
            include("partials/visita", { visita: results[i] }) %> <% } %>
        </main>

    </body>
</html>
